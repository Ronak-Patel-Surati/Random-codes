# other apporches are not leaving the functions aside just gietting the resutl

# need input
def convert():
    line = input().replace(":)" , "🙂").replace(":(","🙁")
    return line
def main():
    print(convert())

main()
